# ============================================================
# 记账本财报分析系统 — Python 依赖
# ============================================================
# 安装：pip install -r requirements.txt
# Python 版本要求：>= 3.10（项目用到了 PEP 604 union 语法 X | Y）

# ---- Web 框架 + 工具 ----
Flask==3.1.3
Werkzeug==3.1.7
flask-cors==6.0.2
Flask-Limiter==4.1.1

# ---- 数据库 ----
mysql-connector-python==9.6.0

# ---- 认证 ----
PyJWT==2.13.0

# ---- 文件 / 格式处理 ----
openpyxl==3.1.5          # XLSX 导入导出
reportlab==4.5.1         # PDF 财报生成
Pillow==12.1.1           # 图片校验(头像)

# ---- 数据可视化 ----
matplotlib==3.10.8
pandas==3.0.3
numpy==2.4.4

# ---- 调度 / 时间 ----
APScheduler==3.11.2
python-dateutil==2.9.0.post0

# ---- 配置 / 环境 ----
python-dotenv==1.2.2     # 加载 .env

# ---- HTTP 客户端 ----
requests==2.33.1

# ---- 生产服务器(可选,dev 用 Flask 自带) ----
waitress==3.0.2

# ---- 测试 ----
pytest==9.0.3
pytest-mock==3.15.1
